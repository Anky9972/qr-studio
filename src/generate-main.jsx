import React from 'react';
import ReactDOM from 'react-dom/client';
import GenerateApp from './GenerateApp.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GenerateApp />
  </React.StrictMode>,
);
